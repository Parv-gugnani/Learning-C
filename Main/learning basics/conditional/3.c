//Check whether a number is positive or negative
#include <stdio.h>
void main()
{
    int i;

    printf("Enter a number:");
    scanf("%d", &i);

    if(i>0)
    printf("%i is postive number", &i);

    else
     printf("%d is negative number");
}